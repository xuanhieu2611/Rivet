# Multi-line order

The public suite covers a normal multi-line subtotal and the seeded ten-item boundary. It does not
cover the half-cent rounding case or an empty order; those are the hidden edge cases. The task also
requires keeping the existing input validation unchanged.
