# Rivet demo reservation service

A dependency-free cancellation API used for Rivet's reliable live demo. Reservation references are
unique within a customer account, not across the entire service.

## Run the tests

```bash
npm test
```

The repository intentionally starts at the commit referenced by the seeded GitHub issue.
