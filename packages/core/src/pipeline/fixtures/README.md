# Test reporter fixtures

These reports were captured from Vitest 4.1.10 and Jest 30.2.0 by running one passing, one failing,
and one skipped test with each runner's JSON reporter. Volatile timestamps, durations, snapshots,
and diagnostic stack traces were removed so the fixtures stay deterministic. The reporter summary,
test-result, assertion, status, name, and absolute-path shapes are unchanged.
