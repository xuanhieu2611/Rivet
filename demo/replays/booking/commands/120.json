{
  "id": 120,
  "phase": "testing",
  "argv": ["git", "diff", "--cached"],
  "cwd": "/home/node/workspace/repo",
  "exitCode": 0,
  "durationMs": 15,
  "stdout": "diff --git a/migrations/001_initial.sql b/migrations/001_initial.sql\nindex dd9a38b..2dfaf72 100644\n--- a/migrations/001_initial.sql\n+++ b/migrations/001_initial.sql\n@@ -13,3 +13,5 @@ CREATE TABLE booking_slots (\n   room_id TEXT NOT NULL,\n   starts_at TEXT NOT NULL\n );\n+\n+CREATE UNIQUE INDEX idx_booking_slots_room_time ON booking_slots(room_id, starts_at);\ndiff --git a/src/app.js b/src/app.js\nindex 862ea78..09a8f3c 100644\n--- a/src/app.js\n+++ b/src/app.js\n@@ -1,5 +1,6 @@\n import { BookingRepository } from \"./booking-repository.js\";\n-import { BookingConflictError, BookingService } from \"./booking-service.js\";\n+import { BookingConflictError } from \"./errors.js\";\n+import { BookingService } from \"./booking-service.js\";\n import { createBookingDatabase } from \"./database.js\";\n \n export function createBookingApp(options = {}) {\ndiff --git a/src/booking-repository.js b/src/booking-repository.js\nindex e320bd0..0cd39d7 100644\n--- a/src/booking-repository.js\n+++ b/src/booking-repository.js\n@@ -1,3 +1,5 @@\n+import { BookingConflictError } from \"./errors.js\";\n+\n export class BookingRepository {\n   constructor(database) {\n     this.database = database;\n@@ -11,18 +13,28 @@ export class BookingRepository {\n   }\n \n   createBooking(booking, slotStarts) {\n-    const inserted = this.database\n-      .prepare(\"INSERT INTO bookings (room_id, guest_name, starts_at, ends_at) VALUES (?, ?, ?, ?)\")\n-      .run(booking.roomId, booking.guestName, booking.startsAt, booking.endsAt);\n-    const bookingId = Number(inserted.lastInsertRowid);\n-    const insertSlot = this.database.prepare(\n-      \"INSERT INTO booking_slots (booking_id, room_id, starts_at) VALUES (?, ?, ?)\",\n-    );\n+    this.database.exec(\"BEGIN\");\n+    try {\n+      const inserted = this.database\n+        .prepare(\"INSERT INTO bookings (room_id, guest_name, starts_at, ends_at) VALUES (?, ?, ?, ?)\")\n+        .run(booking.roomId, booking.guestName, booking.startsAt, booking.endsAt);\n+      const bookingId = Number(inserted.lastInsertRowid);\n+      const insertSlot = this.database.prepare(\n+        \"INSERT INTO booking_slots (booking_id, room_id, starts_at) VALUES (?, ?, ?)\",\n+      );\n \n-    for (const startsAt of slotStarts) {\n-      insertSlot.run(bookingId, booking.roomId, startsAt);\n-    }\n+      for (const startsAt of slotStarts) {\n+        insertSlot.run(bookingId, booking.roomId, startsAt);\n+      }\n \n-    return { id: bookingId, ...booking };\n+      this.database.exec(\"COMMIT\");\n+      return { id: bookingId, ...booking };\n+    } catch (err) {\n+      this.database.exec(\"ROLLBACK\");\n+      if (err.errcode === 2067) {\n+        throw new BookingConflictError();\n+      }\n+      throw err;\n+    }\n   }\n }\ndiff --git a/src/booking-service.js b/src/booking-service.js\nindex e0bdcdc..9e6c6a7 100644\n--- a/src/booking-service.js\n+++ b/src/booking-service.js\n@@ -1,11 +1,6 @@\n-const SLOT_MS = 30 * 60 * 1000;\n+import { BookingConflictError } from \"./errors.js\";\n \n-export class BookingConflictError extends Error {\n-  constructor() {\n-    super(\"room is already booked for part of that time\");\n-    this.name = \"BookingConflictError\";\n-  }\n-}\n+const SLOT_MS = 30 * 60 * 1000;\n \n export class BookingService {\n   constructor(repository, hooks = {}) {\ndiff --git a/src/errors.js b/src/errors.js\nnew file mode 100644\nindex 0000000..3f57106\n--- /dev/null\n+++ b/src/errors.js\n@@ -0,0 +1,6 @@\n+export class BookingConflictError extends Error {\n+  constructor() {\n+    super(\"room is already booked for part of that time\");\n+    this.name = \"BookingConflictError\";\n+  }\n+}\n\\ No newline at end of file\ndiff --git a/test/booking.test.js b/test/booking.test.js\nindex 32cf921..d365c5f 100644\n--- a/test/booking.test.js\n+++ b/test/booking.test.js\n@@ -58,3 +58,115 @@ test(\"rejects a sequential overlapping booking\", async (t) => {\n \n   assert.equal(conflict.status, 409);\n });\n+\n+test(\"concurrent requests for same room produce one 201 and one 409\", async (t) => {\n+  let release;\n+  const gate = new Promise((resolve) => {\n+    release = resolve;\n+  });\n+\n+  const app = createBookingApp({\n+    hooks: {\n+      afterAvailabilityCheck: () => gate,\n+    },\n+  });\n+  t.after(() => app.close());\n+\n+  // Both requests begin; each awaits afterAvailabilityCheck before writing\n+  const resultsPromise = Promise.all([\n+    app.handleRequest({ method: \"POST\", path: \"/bookings\", body: MORNING }),\n+    app.handleRequest({ method: \"POST\", path: \"/bookings\", body: { ...MORNING, guestName: \"Lin\" } }),\n+  ]);\n+\n+  // Allow a microtask tick so both reach the gate\n+  await new Promise((r) => setImmediate(r));\n+\n+  // Release both requests at once so they race into createBooking\n+  release();\n+\n+  const [r1, r2] = await resultsPromise;\n+\n+  const statuses = [r1.status, r2.status].sort();\n+  assert.deepEqual(statuses, [201, 409]);\n+\n+  // Only one booking's slots should exist in the database\n+  const slotCount = app.database\n+    .prepare(\"SELECT COUNT(*) AS cnt FROM booking_slots WHERE room_id = ?\")\n+    .get(MORNING.roomId);\n+  assert.equal(slotCount.cnt, 2);\n+\n+  // No duplicate (room_id, starts_at) rows\n+  const dups = app.database\n+    .prepare(\n+      \"SELECT starts_at, COUNT(*) AS cnt FROM booking_slots WHERE room_id = ? GROUP BY starts_at HAVING cnt > 1\",\n+    )\n+    .all(MORNING.roomId);\n+  assert.equal(dups.length, 0);\n+});\n+\n+test(\"concurrent requests for different rooms both succeed\", async (t) => {\n+  let release;\n+  const gate = new Promise((resolve) => {\n+    release = resolve;\n+  });\n+\n+  const app = createBookingApp({\n+    hooks: {\n+      afterAvailabilityCheck: () => gate,\n+    },\n+  });\n+  t.after(() => app.close());\n+\n+  const resultsPromise = Promise.all([\n+    app.handleRequest({ method: \"POST\", path: \"/bookings\", body: MORNING }),\n+    app.handleRequest({\n+      method: \"POST\",\n+      path: \"/bookings\",\n+      body: { ...MORNING, roomId: \"aurora\", guestName: \"Lin\" },\n+    }),\n+  ]);\n+\n+  await new Promise((r) => setImmediate(r));\n+  release();\n+\n+  const [r1, r2] = await resultsPromise;\n+\n+  assert.equal(r1.status, 201);\n+  assert.equal(r2.status, 201);\n+});\n+\n+test(\"concurrent adjacent bookings for same room both succeed\", async (t) => {\n+  let release;\n+  const gate = new Promise((resolve) => {\n+    release = resolve;\n+  });\n+\n+  const app = createBookingApp({\n+    hooks: {\n+      afterAvailabilityCheck: () => gate,\n+    },\n+  });\n+  t.after(() => app.close());\n+\n+  const resultsPromise = Promise.all([\n+    app.handleRequest({ method: \"POST\", path: \"/bookings\", body: MORNING }),\n+    app.handleRequest({\n+      method: \"POST\",\n+      path: \"/bookings\",\n+      body: {\n+        roomId: MORNING.roomId,\n+        guestName: \"Grace\",\n+        startsAt: MORNING.endsAt,\n+        endsAt: \"2026-05-04T11:00:00.000Z\",\n+      },\n+    }),\n+  ]);\n+\n+  await new Promise((r) => setImmediate(r));\n+  release();\n+\n+  const [r1, r2] = await resultsPromise;\n+\n+  assert.equal(r1.status, 201);\n+  assert.equal(r2.status, 201);\n+});\n",
  "stderr": "",
  "truncated": false,
  "timedOut": false,
  "oomKilled": false
}
